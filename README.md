macOS Finder is pretty bare and not very useful.

I want to be able to see all images in a folder and still have the ability to do normal Finder operations like sort by name, size, data modified, date created, last accessed, kind/type.
